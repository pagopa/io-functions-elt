name: Deploy Pipelines - PROD ITN

on:
  workflow_dispatch: {}
  push:
    branches:
      - main
    paths:
      - "apps/io-functions-elt/CHANGELOG.md"

permissions:
  attestations: write
  id-token: write
  contents: read

jobs:
  deploy:
    name: Deploy
    uses: pagopa/dx/.github/workflows/release-azure-appsvc-v1.yaml@625f02f382c33eb449cd94facb8e426bf7ae37ea # main
    secrets: inherit
    with:
      workspace_name: io-functions-elt
      environment: app-prod
      resource_group_name: io-p-itn-elt-rg-01
      web_app_name: io-p-itn-elt-func-01