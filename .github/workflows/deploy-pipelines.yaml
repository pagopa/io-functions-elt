name: Deploy Pipelines - PROD WEU

on:
  workflow_dispatch:
      
jobs:
  deploy_on_prod:
    uses: pagopa/dx/.github/workflows/legacy_deploy_pipelines.yaml@main
    name: Deploy on PROD WEU
    secrets: inherit
    with:
      environment: 'app-prod'
      resource_group_name: 'io-p-elt-rg'
      app_name: 'io-p-fn-elt'
      use_staging_slot: false
      use_private_agent: true
      health_check_path: '/api/v1/info'
